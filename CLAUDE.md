# [Project Name] — Knowledge OS

## What this vault is
Knowledge operating system for [your project] — built on PARA + Maps of Content, with AI-first note conventions.

## Folder map (PARA)
| Folder | Purpose |
|--------|---------|
| `00-Inbox/` | Capture fast — process weekly |
| `01-Daily/` | Daily briefs and weekly check-ins |
| `02-Projects/` | Active work with goal + deadline |
| `03-Areas/` | Ongoing responsibilities (no deadline) |
| `04-Resources/` | Reference by topic |
| `05-Knowledge/` | Consolidated frameworks + People CRM |
| `06-Wiki/` | Entities, concepts, decisions |
| `07-Maps/` | Maps of Content — index notes linking related notes |
| `08-Archive/` | Completed, inactive, paused |
| `09-agents/` | Agent configs and automation scripts |

## Key files
- `07-Maps/Home.md` — vault entry point
- `02-Projects/<active-project>/` — current active project
- `SOUL.md` — who you are, values, constraints
- `CRITICAL_FACTS.md` — always-loaded key facts

## The app code lives here too — `fridson-app/`
This vault (repo `github.com/westsoever/fridson`) is the **knowledgespace** (planning, maps, team comms). The **actual product** is cloned at `fridson-app/` — a *separate* git repo (`github.com/westsoever/fridson-app`, connected to Lovable).

- ✅ **Agents can edit the app directly** in `fridson-app/` — it's inside the workspace, so reads/writes work with no extra permissions.
- 🔒 **Two repos stay separate.** `fridson-app/` has its own `.git`, its own `origin`, and its own `.lovable` link. It is git-ignored by this vault (`.gitignore`) and excluded from Obsidian (`.obsidian/app.json`) — so vault notes and app code never mix.
- 📦 **Commits/pushes from inside `fridson-app/` go to the app repo** (and flow to Lovable). Commits at the vault root go to the knowledgespace repo. Never `git add` across the boundary.
- 🧭 When a task says "build/change the app", work in `fridson-app/` (stack: Vite + React + TypeScript + Bun + Supabase, see `fridson-app/AGENTS.md`). When it says "plan/document", work in the PARA folders.

## Session skills
| Skill | When |
|-------|------|
| `/session-context` | Start of every session |
| `/plan` | Daily work — pick next task |
| `/wrap-up` | End of session |
| `/new-note` | Capture a new permanent note |

## Hard rules
- Every note goes in the right PARA folder — not the root
- Inbox is processed at least once/week
- Maps link to notes; notes link back to their Map
- Archive rather than delete


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>